## 🚀 COPILOT ШПАРГАЛКА ДЛЯ BRANDEDBY

### ⌨️ ГОРЯЧИЕ КЛАВИШИ

| Функция | Windows/Linux | Mac |
|---------|---------------|-----|
| **Открыть Copilot Chat** | `Ctrl+Shift+I` | `Cmd+Shift+I` |
| **Inline Chat** | `Ctrl+I` | `Cmd+I` |
| **Принять предложение** | `Tab` | `Tab` |
| **Отклонить предложение** | `Esc` | `Esc` |
| **Следующее предложение** | `Alt+]` | `Option+]` |
| **Предыдущее предложение** | `Alt+[` | `Option+[` |

### 💬 CHAT КОМАНДЫ

#### **Базовые команды:**
- `/explain` - объяснить выделенный код
- `/fix` - исправить ошибки в коде  
- `/tests` - создать unit тесты
- `/doc` - создать JSDoc документацию
- `/optimize` - оптимизировать производительность

#### **Специфичные для BrandedBy:**
```
/explain этот компонент анимации лиц
/fix проблемы с мобильной версткой  
/tests создай тесты для логгера
/optimize этот React компонент для производительности
/doc добавь TypeScript типы и документацию
```

### 🎯 ПОЛЕЗНЫЕ ПРОМПТЫ

#### **Создание компонентов:**
```
Создай React компонент для карточки знаменитости с:
- TypeScript типами
- Tailwind CSS стилями  
- Мобильной адаптацией
- Логированием кликов
```

#### **Оптимизация кода:**
```
Оптимизируй этот компонент для:
- Мобильных устройств
- Accessibility (a11y)
- Performance (React.memo, useMemo)
- SEO оптимизации
```

#### **Работа с API:**
```
Создай типы TypeScript для:
- Celebrity API response
- Logger configuration
- EmailJS integration
```

### 🔧 INLINE CHAT ПРИМЕРЫ

#### **В файле компонента (Ctrl+I):**
- "добавь loading состояние"
- "сделай этот компонент responsive" 
- "добавь error handling"
- "оптимизируй для mobile"

#### **В CSS/Tailwind (Ctrl+I):**
- "сделай это адаптивным"
- "добавь hover эффекты"
- "улучши accessibility"

### 🎨 СПЕЦИАЛИЗИРОВАННЫЕ ПРОМПТЫ

#### **Для анимаций:**
```
Создай CSS анимацию для морфинга лиц с:
- Smooth transitions
- Mobile оптимизацией
- Reduced motion support
```

#### **Для форм:**
```
Создай валидацию формы с:
- React Hook Form
- Zod схемой
- Error handling
- Accessibility
```

#### **Для логирования:**
```
Добавь логирование с нашим logger'ом для:
- User interactions
- Error tracking  
- Performance metrics
```

### 💡 PRO СОВЕТЫ

#### **1. Контекст имеет значение:**
- Держите связанные файлы открытыми
- Используйте описательные комментарии
- Указывайте технологии в комментариях

#### **2. Пишите хорошие prompt'ы:**
- Будьте конкретными
- Указывайте технологии (React, TypeScript, Tailwind)
- Описывайте требования полностью

#### **3. Используйте типы TypeScript:**
```typescript
// Copilot лучше понимает с типами
interface CelebrityProps {
  id: string;
  name: string;
  onSelect: (id: string) => void;
}
```

#### **4. Комментируйте сложную логику:**
```typescript
// AI face morphing animation controller
// Handles transitions between celebrity faces with smooth interpolation
const handleFaceMorph = () => {
  // Copilot предложит релевантную реализацию
}
```

### ⚡ БЫСТРЫЙ СТАРТ

1. **Откройте файл** в BrandedBy проекте
2. **Начните печатать** - Copilot предложит код
3. **Используйте Ctrl+I** для inline вопросов
4. **Открывайте Chat (Ctrl+Shift+I)** для сложных задач

### 🎉 Готово!

Copilot теперь знает ваш проект и будет предлагать релевантный код для:
- React компонентов
- TypeScript типов
- Tailwind CSS классов  
- Логирования и обработки ошибок
- Мобильной оптимизации

**Начинайте кодить - Copilot поможет! 🚀**