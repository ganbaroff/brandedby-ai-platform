/**
 * Package Context - Re-exports for backward compatibility
 * Fast Refresh compatible structure
 */

// Re-export context and types
export { PackageContext, type Package, type PackageContextType } from './PackageContext';

// Re-export provider component
export { PackageProvider } from './PackageProvider';
